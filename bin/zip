#!/usr/bin/env bash
set -euo pipefail

ZIP_NAME="tiny-compress-images.zip"

files=(
  "src"
  "README.md"
  "readme.txt"
  "tiny-compress-images.php"
)

for f in "${files[@]}"; do
  echo "  - $f"
done

zip -r "$ZIP_NAME" "${files[@]}"

echo "Done: $ZIP_NAME."
